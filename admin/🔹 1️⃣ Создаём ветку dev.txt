🔹 1️⃣ Создаём ветку dev
✅ В Cursor (самый простой способ)
Внизу слева

У тебя написано:

main*


Нажимаешь на это → откроется список веток →
выбираешь:

Create new branch


Пишешь:

dev


Готово ✅

Или через терминал
git checkout -b dev


Потом:

git push -u origin dev


Теперь в GitHub появится ветка dev.

🔹 2️⃣ Как переключаться между ветками в Cursor
Способ 1 (UI — самый удобный)

Внизу слева:

main


Кликаешь → выбираешь:

dev


Cursor автоматически сделает:

git checkout dev

Способ 2 — через терминал
git checkout dev


или

git checkout main

🔹 3️⃣ Правильная схема работы (твоя логика)

Вот как должно быть 👇

main  → production (сервер)
dev   → тесты / разработка

💡 Как работает процесс
1️⃣ Работаешь в dev

делаешь коммиты

пушишь в dev

деплоишь dev на тестовый сервер

git push origin dev

2️⃣ Когда всё проверено

Делаешь merge:

git checkout main
git merge dev
git push origin main

3️⃣ Сервер слушает только main

Автодеплой настроен на:

branch: main


→ значит сервер обновится только когда ты сделаешь merge в main

🔥 Это правильная архитектура.

🔹 4️⃣ Как другому человеку скачать и выбрать dev
Клонирует проект:
git clone https://github.com/nobilauto-it/Admin-Dashboard.git


Переходит в папку:

cd Admin-Dashboard


Смотрит ветки:

git branch -a


Переключается на dev:

git checkout dev


И всё.

🔹 5️⃣ Важно (чтобы случайно не деплоили dev)

Можно сделать:

GitHub → Settings → Branches

Добавить protection rule:

Protect main branch


И:

запретить прямые пуши

только через Pull Request

Это уже уровень команды 🔥

🔹 6️⃣ Идеальная архитектура для тебя

У тебя 2 сервера:

prod.carsline.md  → слушает main
dev.carsline.md   → слушает dev


И тогда:

пуш в dev → обновляется dev сервер

merge в main → обновляется production

Это уже нормальный CI/CD уровень.

🔥 Итог

Да, ты всё делаешь правильно.

Твоя логика:

dev → тест → merge → main → сервер

Это правильный production workflow.